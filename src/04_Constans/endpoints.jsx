const ENDPOINTS = {
	key: "/route/to/endpoint",
	userInfo: "/users/{user}",
	userRepos: "/users/{user}/repos",
	searchRepos: "/search/repositories?q={query}&per_page={20}",
};
export default ENDPOINTS;
